# Legacy-Path Explicit-Fault Sanity-Check Summary

The directory and filenames retain `ablation` only for compatibility. Each
template directly injects secret release and/or acceptance without a prior run.
`EXPLICIT_FAULT_DETECTED` means the tool/runner recorded that seeded fault; it
does not mean that a single protocol component was removed or that its causal
security contribution was measured. Timings are runner metadata only.

See `ABLATION_SUMMARY.csv` for the preserved per-file evidence excerpts and
`../supplement_S3_logs/S3_MODEL_VALIDITY_STATUS.csv` for authoritative model
interpretation.
