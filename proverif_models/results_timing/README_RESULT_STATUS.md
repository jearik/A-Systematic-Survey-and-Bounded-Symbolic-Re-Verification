# Result Status Notice

The raw timing rows are preserved legacy runner metadata. A raw `PASS` token
means only that the encoded file completed with its local query labels during
that historical run. M1 is PARTIAL under the authoritative query-specific
reruns; M2-M6 and all Tamarin/Scyther family files are ILLUSTRATIVE. Do not use
these timings as proof strength, protocol performance, or cross-tool evidence.
